builder:
  dataset_path: download
  source_env_var: LIBRISPEECH
  required_splits:
    - train-clean-100
    - train-clean-360
    - train-other-500
    - dev-clean
    - dev-other
    - test-clean
    - test-other

dataset:
  supported_splits:
    - train-clean-100
    - train-clean-360
    - train-other-500
    - dev-clean
    - dev-other
    - test-clean
    - test-other

cache:
  enabled: false
  backend: omniio
  cache_dir: data/omniio
